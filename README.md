# 部署

将代码前端部署到 GitHub Pages
1. 下载依赖 `npm install`
2. 生成静态文件 `npm run build`
3. 运行 `npm run deploy` 会自动将生成的静态文件上传到 GitHub Pages
4. 在 GitHub 仓库的 Settings 中找到 GitHub Pages，选择 `gh-pages` 分支，然后点击 `Save`，稍等片刻即可访问
5. 访问 [https://bob-cheung.github.io/money/] 即可访问你的网站


